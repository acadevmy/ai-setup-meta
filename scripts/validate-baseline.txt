# validate-baseline.txt — known failures of scripts/validate-plugin.sh.
#
# One line per finding: CHECK_ID <TAB> KEY. The entries listed here are
# reported but do not fail CI. Every PR that fixes a defect removes the
# matching line: the baseline shrinks, it does not grow.
# Regenerate with: bash scripts/validate-plugin.sh --update-baseline
#
# The repo's real state: bash scripts/validate-plugin.sh --strict
#
# Empty since DE-16478: the last entries were the line/word budgets and the
# missing description triggers, which the progressive-disclosure PR cleared.
# A new finding gets fixed, not added here.
