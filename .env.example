# Copiare questo file in .env.local e compilare i valori
# .env.local e' gitignored — non committare mai le chiavi reali

# GitHub
GITHUB_ORG=                      # Nome organizzazione GitHub (es. my-company)
GITHUB_DIST_REPO=                # Repo di distribuzione (es. ai-setup-dist)